language: cpp
install: 
  - cd ..
  - mkdir libs && cd libs
  - mkdir fmod && cd fmod
  - cp ../../fmod-gdnative/getFmodAPIScript.sh ./
  - ./getFmodAPIScript.sh $FMODUSER $FMODPASS
  - tar -xvf -C linux fmodstudioapi11012linux.tar.gz